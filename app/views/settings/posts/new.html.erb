<div class="page-header">
  <h1><%= @blog.name%></h1>
  <h2 style="color: #00BF32">
    <%= t('crud.new!', resource: Post.model_name.human).titleize %>
  </h2>
</div>

<%= render 'form' %>

<%= link_to t('action.back'), @blog %>
