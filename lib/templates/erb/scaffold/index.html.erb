<div class="page-header">
  <%%= link_to_if can?(:create, <%= class_name %>), t('crud.new!', resource: <%= class_name %>.model_name.human), new_<%= singular_table_name %>_path, class: 'btn btn-primary btn-lg pull-right' %>
  <h1><%%= <%= class_name %>.model_name.human %></h1>
</div>

<%%= paginate @<%= plural_table_name %> %>

<%%= table_for @<%= plural_table_name %>, <%= attributes.reject(&:password_digest?).map{ |attr| ":#{attr.name}" }.join(', ') %>, class: 'table table-striped' %>

<%%= paginate @<%= plural_table_name %> %>