<%%= dl_for @<%= singular_table_name %>, <%= attributes.reject(&:password_digest?).map{ |attr| ":#{attr.name}" }.join(', ') %> %>
<%%= link_to_if can?(:edit, @<%= singular_table_name %>), t('crud.edit'), edit_<%= singular_table_name %>_path(@<%= singular_table_name %>) %> |
<%%= link_to t('action.back'), <%= index_helper %>_path %>
